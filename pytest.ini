[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure logic, no I/O, no API keys
    integration: uses local files or in-memory SQLite, no external APIs
    external: requires live external API (Groq, HuggingFace, E2B) — skipped in CI
    postgres: requires a live PostgreSQL connection — skipped in CI
